<div class="container mt-5">
  <div class="card shadow-sm">
    <div class="card-header bg-warning text-white">
      <h1>Edit Service</h1>
    </div>
    <div class="card-body">
      <%= render "form", service: @service %>
    </div>
  </div>

  <div class="mt-4">
    <%= link_to "Back", new_service_path, class: "btn btn-secondary" %>
  </div>
</div>
